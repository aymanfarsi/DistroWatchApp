import 'package:flutter/material.dart';

class SearchDistro extends StatelessWidget {
  const SearchDistro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SearchDistro'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class LatestDistros extends StatelessWidget {
  const LatestDistros({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Latest Distros'),
      ),
    );
  }
}

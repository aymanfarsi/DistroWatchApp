import 'package:flutter/material.dart';

class RandomDistro extends StatelessWidget {
  const RandomDistro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('RandomDistro'),
      ),
    );
  }
}
